[pytest]
testpaths = tests
python_files = test_*.py
asyncio_mode = auto
timeout = 60
addopts =
    --verbose
    --strict-config
    --strict-markers
markers =
    integration: exercises the complete ASGI or MCP boundary
