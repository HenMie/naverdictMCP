# Naver Dictionary MCP Server - 示例环境变量配置
#
# 说明：
# - APP_ENV=development 时会自动读取本地 .env（便于开发）
# - pytest 运行时会自动进入 testing，不会读取 .env（避免本地配置污染测试）
# - APP_ENV=production 不会读取 .env，建议由部署平台/容器注入环境变量

# 运行模式：development/testing/production（或 dev/test/prod）
APP_ENV=development

# 服务器监听地址与端口
SERVER_HOST=0.0.0.0
SERVER_PORT=8000

# HTTP 请求超时（秒）
HTTP_TIMEOUT=30.0

# 日志级别：DEBUG/INFO/WARNING/ERROR/CRITICAL
LOG_LEVEL=INFO

# Naver API 基础 URL
NAVER_BASE_URL=https://korean.dict.naver.com/api3

# 上游（Naver）请求限流（每分钟，**全局共享**）
REQUESTS_PER_MINUTE=60

# 缓存配置
# - CACHE_TTL：缓存 TTL（秒）
# - CACHE_MAX_SIZE：缓存最大条目数
CACHE_TTL=3600
CACHE_MAX_SIZE=1000

# httpx 连接池配置
HTTPX_MAX_KEEPALIVE_CONNECTIONS=20
HTTPX_MAX_CONNECTIONS=100
HTTPX_KEEPALIVE_EXPIRY=30.0

# 批量查询内部并发上限（仅限制访问上游的瞬时并发）
BATCH_CONCURRENCY=5